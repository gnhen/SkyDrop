# ⚠️ WARNING: CHANGE THESE CREDENTIALS BEFORE DEPLOYING TO PRODUCTION! ⚠️
#
# These are default values for easy testing and development.
# For production use:
#   1. Generate secure secret key: python -c "import secrets; print(secrets.token_hex(32))"
#   2. Use strong password: python -c "import secrets; print(secrets.token_urlsafe(16))"
#   3. Update values below and restart the application

# Flask Configuration
FLASK_APP=receiver.py
FLASK_ENV=production
SECRET_KEY=please-change-this-secret-key-in-production

# Authentication - ⚠️ CHANGE THESE IMMEDIATELY! ⚠️
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Application Settings
MAX_CONTENT_LENGTH=16777216
SAVE_DIR=received_files

# Database
DATABASE_URL=sqlite:///skydrop.db

# Rate Limiting
RATELIMIT_STORAGE_URL=memory://

# Server Settings
HOST=0.0.0.0
PORT=5000
